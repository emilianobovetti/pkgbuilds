pkgname=llama.cpp-cuda
pkgver=b9018
pkgrel=1
# latest commit (git rev-parse --short HEAD)
# https://github.com/ggml-org/llama.cpp/commits/b9018/
_commit_id=c84e6d6
# raw commit number (git rev-list --count HEAD)
# https://github.com/ggml-org/llama.cpp/blob/b9018/.github/workflows/build-cmake-pkg.yml#L31
_build_number=9018
pkgdesc="LLM inference in C/C++"
arch=(x86_64 armv7h aarch64)
url="https://github.com/ggerganov/llama.cpp"
license=(MIT)
depends=(
  cuda
  curl
  gcc-libs
  glibc
  nvidia-utils
)
makedepends=(
  cmake
  gcc14
  ninja
)
optdepends=(
  "python-numpy: needed for convert_hf_to_gguf.py"
  "python-safetensors: needed for convert_hf_to_gguf.py"
  "python-sentencepiece: needed for convert_hf_to_gguf.py"
  "python-pytorch: needed for convert_hf_to_gguf.py"
  "python-transformers: needed for convert_hf_to_gguf.py"
)
provides=(llama.cpp)
conflicts=(llama.cpp libggml ggml)
source=("${pkgname}-${pkgver}.tar.gz::https://github.com/ggml-org/llama.cpp/archive/refs/tags/${pkgver}.tar.gz")
sha256sums=('0cd6c8d07ce4f316b5172fe9cfc07ea0d74744bcacaa2dde01512f634c496074')

build() {
  export NVCC_CCBIN="/usr/bin/g++-14"
  export CC="/usr/bin/gcc-14"
  export CXX="/usr/bin/g++-14"
  export CUDAHOSTCXX="/usr/bin/g++-14"

  cmake \
    -B build \
    -G Ninja \
    -S "${srcdir}/llama.cpp-${pkgver}" \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX='/usr' \
    -DBUILD_SHARED_LIBS=ON \
    -DLLAMA_BUILD_TESTS=OFF \
    -DLLAMA_USE_SYSTEM_GGML=OFF \
    -DGGML_ALL_WARNINGS=OFF \
    -DGGML_ALL_WARNINGS_3RD_PARTY=OFF \
    -DGGML_BUILD_EXAMPLES=OFF \
    -DGGML_BUILD_TESTS=OFF \
    -DGGML_OPENMP=ON \
    -DGGML_LTO=ON \
    -DGGML_RPC=ON \
    -DGGML_CUDA=ON \
    -DGGML_CUDA_FA_ALL_QUANTS=ON \
    -DGGML_CUDNN=ON \
    -DGGML_CUDA_COMPRESSION_MODE=speed \
    -DLLAMA_BUILD_SERVER=ON \
    -DLLAMA_BUILD_COMMIT="${_commit_id}" \
    -DLLAMA_BUILD_NUMBER="${_build_number}" \
    -DLLAMA_OPENSSL=ON \
    -DGGML_NATIVE=ON \
    -Wno-dev

  cmake --build build
}

package() {
  DESTDIR="${pkgdir}" cmake --install build

  install -Dm644 \
    "${srcdir}/llama.cpp-${pkgver}/LICENSE" \
    "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
